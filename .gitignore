.workbuddy/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.pytest_tmp/
.venv/
venv/
.env
# content/ = 运行态世界与存档（一个世界就是一份存档），整目录不进版本库。
# 于是新克隆的仓库 / 便携包里都没有 content/；主程序会在建世界时自动创建它
# （回归测试见 tests/test_empty_content.py）。
/content/
web/node_modules/
web/dist/.vite/
*.log
scratch_saves/
bt_all/
bt_tt/
data/settings.json
# presets.json = 你调好的写作预设（文风准则 / 屏蔽词 / 样本），运行态、可能含个人内容，
# 不进版本库。缺文件时首次运行会写一份 DEFAULT_PRESET（见 app/core/presets.py）。
data/presets.json
backup_content_*.tar.gz
backup_content_*.zip
